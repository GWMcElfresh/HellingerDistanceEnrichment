# Build contrast specifications from group levels and custom overrides.

Build contrast specifications from group levels and custom overrides.

## Usage

``` r
build_contrast_specifications(group, custom_contrasts = NULL)
```
