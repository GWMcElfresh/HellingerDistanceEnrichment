# Apply subset and collapse transforms to a group factor for one contrast.

Apply subset and collapse transforms to a group factor for one contrast.

## Usage

``` r
prepare_contrast_group(group, spec)
```
