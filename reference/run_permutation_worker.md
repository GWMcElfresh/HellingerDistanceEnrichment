# Parallel worker wrapper for permutation contrasts.

Parallel worker wrapper for permutation contrasts.

## Usage

``` r
run_permutation_worker(spec, counts, group, n_permutations, prior_pseudocounts)
```
