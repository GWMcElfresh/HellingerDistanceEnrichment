# Observed effect size and ratio for one contrast at fixed counts.

Observed effect size and ratio for one contrast at fixed counts.

## Usage

``` r
compute_observed_contrast(counts, group, spec, prior_pseudocounts)
```
