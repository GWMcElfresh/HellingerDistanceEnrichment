# Hellinger distance between two count vectors (frequencies derived internally).

Hellinger distance between two count vectors (frequencies derived
internally).

## Usage

``` r
compute_hellinger_distance_counts(counts1, counts2)
```

## Arguments

- counts1:

  Numeric count vector.

- counts2:

  Numeric count vector.

## Value

Scalar Hellinger distance.
